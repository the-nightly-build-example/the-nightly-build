# An ordered course. The agent reads the already-published lessons before
# writing the next one; recaps must reference them concretely.
name: Transformers, From Scratch
mode: sequence
template: lesson
prompt: prompt.md
autopublish: true
strict: false
items:
  - slug: attention
    title: Attention Is the Engine
  - slug: from-gpt-to-frontier
    title: From GPT to Frontier Models
  # Extend the syllabus by appending lessons:
  # - slug: post-training
  #   title: "Post-Training: RLHF and Friends"
  # - slug: inference
  #   title: Why Inference Is the Hard Part

# Quality knobs (uncomment to tighten — bands may never loosen below the
# registry floor in templates/registry.yaml):
# words: [2000, 4000]
# min_sources: 10
# strict: true          # promotes every WARN to a BLOCK: thin editions never publish
