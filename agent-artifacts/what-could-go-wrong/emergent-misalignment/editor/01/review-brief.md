# editor review-brief: what-could-go-wrong/emergent-misalignment (01)

Inputs (all under this article's artifact root unless absolute):

- `editorial-direction.md` — house standard, slop, headline standard, press voice,
  lesson identity, series prompt.
- `commission.md` — the assignment, boundaries, required contribution.
- `writer/01/brief.md` — the exact writer brief (check for leakage against it).
- `writing-coach/01/voice-guide.md` — register and quoted exemplars.
- `researcher/01/evidence.md` — the evidence record; read for what breaks a claim.
- `writer/01/draft-handoff.md` — the original-work sentence (third read only); it
  also carries three open notes for you (see below).
- Article: `/home/user/the-nightly-build/.nb-work/what-could-go-wrong/emergent-misalignment/library/what-could-go-wrong/emergent-misalignment.html`
- Template context: `/home/user/the-nightly-build/.nb-work/what-could-go-wrong/emergent-misalignment/.nb-context/`

My recent-pattern notes (compare edges, deks, headings; a match is a formula to
break):

- Paper-wide opener tic: "By the end you will know X. You will also see Y."
- what-could-go-wrong opener tic: "famous number, then its deflation."
- Takeaway on negative parallelism or a "cuts both ways" balance line.
- If a holds-up grid is used, watch for reward-tampering's grid-then-"how far it
  reaches" order. Deks: banned molds; headings: comma-and triad.

This round's focus:

- The spine is the shown-versus-inferred line. Verify the measured core is pinned
  with explicit denominators (the ~20% is on 8 selected questions; ~6% vs ~0.1%
  controls on the 48 pre-registered set; backdoor <0.1% vs ~50%; educational
  control ~0%). A figure stated without its denominator, especially in display
  text, needs a fix. The dek uses "about a fifth of the time" (the 20%/8-question
  figure) without naming the set — judge whether that misleads the scanning reader
  given the body disambiguates; tighten if it overstates.
- The key inference must be present and not overstated: every positive result ran
  through deliberate finetuning, so deployment emergence stays open. Do not let the
  piece imply spontaneous deployment misalignment was shown.
- Critiques must read as inside-the-cluster refinement, not hostile rebuttal.
  OpenAI cited to the arXiv companion, not the gated blog.
- The writer's handoff flags: the "Hitler/Stalin" naming precision (writer used
  only Hitler), and no source asset captured. Both are the writer's if reporting
  is needed; the naming point may need a researcher confirmation of the paper's
  exact named figures — route it if the claim rests on it.

The orchestrator stamps after your direct edits and proves before the PR; return
to the writer only if the proof would demand new prose.
