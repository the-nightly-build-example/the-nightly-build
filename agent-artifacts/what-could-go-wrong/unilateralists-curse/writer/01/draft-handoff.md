# Draft handoff: what-could-go-wrong/unilateralists-curse (01)

## Original work

This lesson converts the evidence record's separate assumption list and
contradiction notes into a single four-assumption test a newcomer can apply,
then runs GPT-2, the Llama releases, and the anti-open-release governance
literature through that test to hold the proven decision-theoretic result apart
from the release analogy.

## Proof result

`./nb check ... --series what-could-go-wrong --library <checkout>` with links on:

```
BLOCK: 0
WARN:  0
verdict: PUBLISHABLE
```

No warnings left standing. Three W-SENTENCE-DENSITY warnings from the first pass
were resolved by splitting the sentences, not waived. Em-dash count is 0 (the
"−1" in the figure caption is a minus sign, locators use en-dashes).

Guardrail honored: no claim that governance authorities routinely invoke "the
unilateralist's curse" by name for current releases. The body states the
opposite and explicitly ("Neither invokes the curse by name"), and GPT-2 and the
Llama releases are staged as structural analogies ("It is a shape, not a
proof"), never as proven instances. No fabricated Shevlane–Dafoe quote appears.

## Furniture

Three pieces, each in a different section and each earning its place: the
captured source asset (Figure 1 of the 2016 paper, CC-BY, cropped to the chart
with printed caption removed) carries the core curve; the table carries the
shown-vs-analogy test (four assumptions vs a real release); the timeline carries
the GPT-2 withhold → OpenGPT-2 replication → OpenAI release sequence.

## Open questions

- Source 4 (TechXplore, secondary corroboration of the OpenGPT-2 replication)
  is titled with a factual paraphrase, not the outlet's verified headline; the
  URL resolves (links-on proof passed). The replicators' own post (s3, primary)
  already owns the fact, so if the editor prefers, s4 can be pruned without loss.
