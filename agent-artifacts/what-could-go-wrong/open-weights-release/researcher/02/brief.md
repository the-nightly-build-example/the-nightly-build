# researcher brief: what-could-go-wrong/open-weights-release (02)

Inputs:
- editorial-direction.md (../../editorial-direction.md)
- the prior evidence record (../01/evidence.md) — preserve all still-valid work
- the editor review (../../editor/01/editorial-review.md) — the finding to fix

Output: evidence.md (this directory) — a complete new evidence record preserving
still-valid 01 work with the one correction below.

Narrow correction only. The 01 record carries a spliced Zuckerberg quotation:
it prints "larger institutions...check the power of smaller bad actors," but the
editor found the primary's clause is "larger actors can check the power of smaller
bad actors" — "larger institutions" was joined in from a different sentence. Fix
exactly this:

- Reopen Zuckerberg's open-source essay (the primary the quote is cited to) firsthand
  and record the exact verbatim wording of the relevant sentence(s). If the point
  the article makes spans two sentences in the source, record both verbatim and
  mark clearly that they are separate sentences, so the writer does not splice them.
- Correct the quote in the source entry / Quote field and anywhere else in the
  record it appears, so nothing propagates the splice.

Everything else in the 01 record verified clean by the editor (the Qi et al. table
numbers, the RAND qualitative null, Seger, Kapoor/Narayanan, the Meta pivot). The
editor also removed a wrong Seger publication month in the article; if the record
has a clean sourced date for the GovAI report (October 2023), preserve/record it.
Keep the source floor (>=8 sources, >=4 primary, >=1 secondary). Confirm every URL
resolves to the source's own page. Do NOT add any operational CBRN content. Report
the corrected verbatim wording and its source.
