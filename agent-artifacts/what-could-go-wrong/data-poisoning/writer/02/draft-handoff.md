# draft-handoff: what-could-go-wrong/data-poisoning (writer/02) — revision

Original-work sentence: The article assembles the four poisoning
demonstrations side by side in a table whose columns are arranged so the
non-composition of the two scariest results (easy-to-install vs.
survives-safety-training) is visible rather than asserted, then names the
gap in both directions.

## Editorial requests resolved (round-01)

- Removed the body-closing "Verdict" `nb-note-strong` block from the
  why-they-dont-combine section; the section now ends on its argument and no
  replacement summarizing block was added (takeaway bookend still lands the
  judgment).
- Corrected s5 (OATML) source title to the verbatim H1 "Poisoning Attacks on
  LLMs Require a Near-constant Number of Poison Samples," disambiguated in the
  source line as the blog announcement of the s3 paper.
- Corrected s8 (Fortune) source title to the verbatim headline "A small amount
  of bad data can 'poison' even the largest AI models, researchers warn."
- Restored the Mavroudis quotation to Fortune's verbatim wording, restoring the
  second "it" and marking the "for example," elision: "when … it detects a
  specific sequence of words, it foregoes its safety training."

## Proof

`./nb check … --series what-could-go-wrong --library /home/user/library-checkout`
(links included) after `./nb stamp`: BLOCK: 0, WARN: 0, PUBLISHABLE.
Stamp: words=2156, reading_minutes=9, sources=8 (under the 2200 ceiling).

No warnings intentionally left. Claim set unchanged; settled work and the
does-not-compose discipline preserved. No open evidence or voice questions.
