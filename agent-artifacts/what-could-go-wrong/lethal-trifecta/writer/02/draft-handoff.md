# writer handoff: what-could-go-wrong/lethal-trifecta (02, revision)

Applied the two required items from editor/01/editorial-review.md. No other prose re-edited; claim set unchanged.

- Invariant misquote (s2): restored the dropped words so the quotation now reads verbatim, "GitHub alone cannot resolve this vulnerability through server-side patches."
- EchoLeak email channel: re-attributed the "crafted email" delivery detail to s4 (Willison's EchoLeak post, which establishes the email vector). The no-interaction/zero-click claim stays under the CVE (s3, UI:N); the sentence was split so each clause is cited to the source that owns it, and "same record" was made "CVE record" to keep the unproven-exploit antecedent unambiguous.

Proof: run by the orchestrator per brief.
