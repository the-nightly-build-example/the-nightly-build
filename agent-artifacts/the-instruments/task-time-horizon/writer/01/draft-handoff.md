# Draft handoff: the-instruments/task-time-horizon (01)

## Original work
The article derives in front of the reader why the 50% horizon falls out of the
logistic fit (when a task's human time equals the horizon, the log difference is
zero and the sigmoid returns exactly one half), then uses that mechanism to split
the measured doubling from the extrapolated calendar and to trace the over-read
back to the paper's own five-year projection rather than to an outside error the
evidence record lists these as separate facts but does not connect.

## Proof
`./nb check ... --series the-instruments --library /home/user/library-checkout`
with links included: **BLOCK: 0, WARN: 0, PUBLISHABLE.** Stamped words=2200
(band 1200-2200), sources=8, reading_minutes=10. Zero em-dashes (limit 4); no
`leverage`/`load-bearing` or press-banned terms. No warnings intentionally left.

Sources: 7 primary (Kwa et al.; METR March blog; METR "Vary Across Domains";
METR "Time Horizon 1.1"; Ord arXiv; Ord half-life writeup; AI Digest for its own
over-read), 1 secondary (Davis review). The two Ord entries are one origin / one
independent critic, as the evidence record flags; both are cited because each
owns a distinct figure the piece uses (threshold ratios vs. the human-decay
numbers).

## Furniture
Source asset (Figure 1) captured with `nb asset` from the arXiv HTML v1 headline
plot, kept full-range per the crop guidance (log axis, all points, trend line,
and the dashed extrapolation past the last model). Annotated equation for the
logistic fit; stat strip (59 min / 212 days); table for the AI Digest milestone
calendar.

## Notes for the editor
- The captured Figure 1's own in-image title reads "169 ... tasks"; the paper
  text and evidence record give 170 (HCAST 97 + RE-Bench 7 + SWAA 66). Prose uses
  170 throughout and does not cite the figure's count. The image is the authentic
  primary artifact and was left unaltered; the caption states no task count.
- Per the brief, no per-model horizon values are quoted (Figure-1 readings with
  wide error bars). Only the record's firm figures are used: Claude 3.7 Sonnet
  59 min (50%) and 15 min (80%), 212-day full-period doubling (95% CI 171-249),
  and the revised 131-day post-2023 figure attributed partly to task composition.

## Open questions
None blocking. No fact the piece needs was missing from the evidence record, so
no researcher request was routed.
