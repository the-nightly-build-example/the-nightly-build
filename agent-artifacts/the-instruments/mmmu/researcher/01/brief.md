# researcher brief: the-instruments/mmmu (01)

Inputs:
- /home/user/the-nightly-build/.nb-work/the-instruments/mmmu/agent-artifacts/the-instruments/mmmu/editorial-direction.md
  — citation standard, series territory, declared reader.
- /home/user/the-nightly-build/.nb-work/the-instruments/mmmu/agent-artifacts/the-instruments/mmmu/commission.md
  — the measurement, the angle, and the named sources to read first.

Output: /home/user/the-nightly-build/.nb-work/the-instruments/mmmu/agent-artifacts/the-instruments/mmmu/researcher/01/evidence.md

Proof of your own record: every URL you record must resolve to the source's own
page (the paper's arXiv or proceedings page, the model card's own page), not a
fetch endpoint; every figure must be checked against the primary that owns it;
nothing may appear that you did not open. The writer and editor will cite only
from this record.

This round's focus: the commission's angle turns on how much of an MMMU score
belongs to vision versus to text and to the multiple-choice format. Establish
firsthand, from the MMMU paper: the construction (question count, disciplines and
subjects, question sources, image-type variety, answer formats) and the paper's
own text-only / vision-blind baseline numbers. Then, from the MMMU-Pro paper:
exactly what it changed (filtering text-answerable questions, augmenting the
options, the vision-only input setting) and the score changes it reports for named
models against plain MMMU. Then quote at least two named models' own MMMU claims
from whoever made them (model card or technical report), so the "number in
circulation" is sourced to its owner, not a leaderboard. Search for what breaks
the angle: a defense of MMMU's validity, or evidence that the text-answerable
share is small, and record it in full. Classify a lab's report of its own model's
score as primary for "what was claimed" and secondary for "whether it reflects
multimodal reasoning." Preserve any full per-model MMMU-versus-MMMU-Pro series a
chart might use.

Do not cite tonight's sibling articles; link targets for the writer must be
already-published library pages only.
