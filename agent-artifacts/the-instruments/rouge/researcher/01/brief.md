# researcher brief: the-instruments/rouge (01)

Inputs:

- `commission.md` (at the artifact root) — the measurement, angle, source policy,
  neighbors, and what the piece must establish.
- `editorial-direction.md` (at the artifact root) — the citation standard, series
  territory, and declared reader.

Output: `researcher/01/evidence.md` (under this article's artifact root).

Answer these specific questions from the primary documents:

- ROUGE's definition from Lin 2004: ROUGE-N and ROUGE-L precisely (recall
  orientation, n-gram overlap, longest common subsequence), quoted and located.
  Work out one tiny, correct worked example (a candidate vs a reference) so the
  lesson can show the arithmetic; record the exact counts.
- The evaluation context it was built for (DUC/TAC), and what "reference"
  summaries are, so the reader understands what the overlap is measured against.
- The misled case with numbers: the meta-evaluations (Fabbri et al. "SummEval"
  2021; Bhandari et al. 2020) on how weakly ROUGE correlates with human judgments
  of quality/consistency, and evidence that ROUGE rewards lexical/extractive
  overlap and does not measure faithfulness. Pin the specific correlation figures
  to their owning primary with the Numbers shape.
- A concrete instance of ROUGE being used as the headline quality claim (a model
  card or paper reporting ROUGE as evidence a system summarizes well), so the
  "still in circulation" point is reported, not asserted.
- Contradictions: record where ROUGE is defended (its reproducibility, its use
  when references are strong) and where critics disagree, in full. Two retellings
  of one origin count once.

Meet the source policy in the commission with primary sources that own each claim.
Every URL must resolve to the source's own page.
