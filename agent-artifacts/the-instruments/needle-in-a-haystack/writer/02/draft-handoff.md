# Draft handoff: the-instruments/needle-in-a-haystack (writer/02) — revision

Original work: the piece decomposes a single near-perfect NIAH score into two
separate skills (finding a planted fact vs. using the whole document) and shows
four harder primaries each removing one crutch the easy score leaned on —
work no single source performs. Unchanged this round.

## Required items resolved

- (blocking) Verbatim-quote fix: the orientation's quotation-marked question,
  cited to #2 (Arize), read "What is the most fun thing to do in San Francisco?"
  — a string in no cited source. Changed it to Arize/Kamradt's exact wording,
  "What is the best thing to do in San Francisco?", keeping the #2 citation. The
  needle sentence (already verbatim-correct) is untouched.
- (blocking) Reconciled with Fig. 1: the figure is the distinct Anthropic
  Claude 2.1 run (#8), whose prompt image shows the differently-worded, longer
  question ("...most fun thing to do in San Francisco based on the context?
  Don't give information outside the document or repeat your findings"). The
  orientation now quotes the original 2023 construction (best-thing, #2) and the
  figure shows the Anthropic run (#8); each printed/shown string is verbatim to
  its own cited source, and no prose equates the two runs. No non-verbatim
  quoted string was introduced.
- (minor) Varied one of the two "One..." headings: "One run becomes a grid" ->
  "A single run becomes a grid" (the orientation heading "One planted sentence"
  is kept as the thematic opener).
- (minor) Removed the noun/verb misread: "Each of those steps past plain
  retrieval..." -> "Each of those tasks steps past plain retrieval..." ("steps"
  now unambiguously reads as the verb).

## Proof

Full `nb check` with links: BLOCK: 0, WARN: 0, verdict PUBLISHABLE.
`nb stamp`: words=1819, reading_minutes=8, sources=8. No warnings left standing.
No open evidence or voice questions; claim set unchanged.
