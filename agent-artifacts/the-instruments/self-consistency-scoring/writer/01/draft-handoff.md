# Draft handoff: the-instruments/self-consistency-scoring (writer 01)

## Original-work sentence

The article turns the evidence's bare cons@k figures into a worked ten-sample
count that shows mechanically why a plurality vote raises a score and precisely
when it cannot, then uses that mechanism to relocate the misread from lab
concealment to circulation, reading Minerva's own Table 2 (a voted 50.3% one row
above a single-try 6.9%) as the clean in-source instance of the mismatch.

## Spine correction applied

The piece does not claim labs hide the compute. It states plainly that in all
three primary cases the single-try and majority-vote numbers sit side by side and
labeled, that DeepSeek's 86.7% is a fair cons@64-to-cons@64 comparison at its own
table, and locates the failure downstream: a voted number quoted without its k or
set beside a single-try score, with Minerva's 50.3%-above-6.9% column as the
in-source example and Artificial Analysis's move to pass@1 as the corrective.

## Proof result

`nb stamp`: words 1969, reading 9 min, sources 8 (6 primary, 2 secondary).

Full `nb check` with links (series the-instruments, against the library
checkout): **BLOCK: 0, WARN: 0, verdict PUBLISHABLE.** No warnings left standing.

## Open evidence / voice questions

- Source 1 (OpenAI o1 page) passed the link check here, but the evidence record
  notes the live URL returns a Cloudflare 403 to automated fetches and resolves
  only in a browser; content was verified from an Internet Archive capture of the
  same URL. If CI's link probe runs from a stricter network it may flag this one
  source. The canonical openai.com URL was kept per the evidence record's
  instruction that this is the source's own page.
- No open voice question. The worked ten-sample count is invented for teaching
  (framed as a supposition, following the voice guide's Our World in Data model)
  and carries no citation by design; the real lift it illustrates is anchored to
  Wang's cited GSM8K result (56.5% to 74.4%).
