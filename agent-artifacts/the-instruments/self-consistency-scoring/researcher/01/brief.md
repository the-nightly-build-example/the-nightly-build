# researcher brief: the-instruments/self-consistency-scoring (01)

Inputs:
- ../../editorial-direction.md — citation standard, series territory, reader
- ../../commission.md — the assignment, the documents to read, the source floor

Output: evidence.md (this directory)

This round's focus: build the full evidence record for the first draft. Confirm the
procedure firsthand from Wang and coauthors' self-consistency paper (how the
plurality vote is taken, the accuracy gains reported, the k values used). Find a
real, citable instance of a cons@k / maj@k / self-consistency number published as a
headline (a model report or system card) and record the exact k, the benchmark, and
whether a single-sample number was reported beside it. Verify the compute cost of
majority voting and any analysis comparing it to single-sample accuracy. Under
Contradictions, record any defense of majority-vote reporting or evidence it does
reflect deployed use. Every URL must resolve to the source's own page.
