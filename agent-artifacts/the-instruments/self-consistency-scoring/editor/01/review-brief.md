# review brief: the-instruments/self-consistency-scoring (editor/01)

Inputs (artifact root is this file's grandparent directory):
- ../../editorial-direction.md — house standard, slop, headlines, paper voice, template identity
- ../../commission.md — the assignment and its boundaries
- ../../writer/01/brief.md — the exact writer brief (check for leakage against it)
- ../../writing-coach/01/voice-guide.md — read first; the register and the quoted exemplars
- ../../researcher/01/evidence.md — the claim set the draft must not exceed
- ../../writer/01/draft-handoff.md — open the original-work sentence only on the third read
- The article: /home/user/the-nightly-build/.nb-work/the-instruments/self-consistency-scoring/library/the-instruments/self-consistency-scoring.html
- Template context: /home/user/the-nightly-build/.nb-work/the-instruments/self-consistency-scoring/.nb-context/

Output: editorial-review.md (this directory). Decision: approve | revise.

## This round's focus (start here)

- Press rule, publication-blocking: the body must not close with a Verdict note or
  any block that restates the finding — the takeaway bookend lands the judgment.
  If any such block is present, remove it.
- Verify the spine is the corrected one: the misread lives in downstream circulation
  and cross-model comparison, NOT in labs concealing compute (all three primary
  cases reported single-sample beside the voted number and labeled the metric).
  Minerva's Table 2 (voted 50.3% one row above a single-try 6.9%) is the in-source
  instance. Check every cons@k / pass@1 figure against the evidence record (Wang
  GSM8K 56.5→74.4; Minerva 33.6→50.3 at k=64; o1 AIME 74 vs 83; DeepSeek-R1-Zero
  71.0 vs 86.7).
- Distinctness: confirm the piece stays clear of humaneval-pass-at-k (pass@k is a
  different rule), aime, and math-benchmark — Background links, not retread.
- Link note (not blocking): the OpenAI o1 source URL returns a bot 403 (verified via
  archive; canonical URL kept). A 403 is restricted, not a 404/410, so the proof's
  link check does not block on it. No action needed unless the citation is wrong.

## Recent-pattern notes (compare against these; one article cannot show a formula)

- Recent The Instruments deks lead with a numeric contrast in one long sentence;
  check this dek leads on the convention instead.
- Recent headings lead with a raw number or use the comma-and contrast; break any.
- Apply the headline spec's banned dek molds (semicolon reversal, suspended
  question, comma triad).
- Furniture: the recurring stack is nb-stat-strip, nb-figure, nb-table, nb-note,
  nb-holdsup. The draft is deliberately lean; confirm what remains earns its place.
