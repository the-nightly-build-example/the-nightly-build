# writer brief: the-instruments/mteb (01)

Inputs:
- ../../editorial-direction.md — house standard, press voice, series prompt
- ../../commission.md — the measurement, the angle, boundaries, source policy, and the recent habits not to inherit
- ../../writing-coach/01/voice-guide.md — how this piece should sound, with verified exemplar passages
- ../../researcher/01/evidence.md — the complete set of claims available to you; use its Numbers section exactly
- article to edit: /home/user/the-nightly-build/.nb-work/the-instruments/mteb/library/the-instruments/mteb.html
- template context: /home/user/the-nightly-build/.nb-work/the-instruments/mteb/.nb-context

Output: draft-handoff.md (this directory)

Proof: ./nb check /home/user/the-nightly-build/.nb-work/the-instruments/mteb/library/the-instruments/mteb.html --series the-instruments --library /home/user/library-checkout

This round's focus: the timeline is the trap, and the record's Contradictions
section governs it. Pin every count to MTEB v3 (8 task types, 58 datasets, 112
languages, 33 models). Task imbalance was named by the original 2022 authors in
their own limitations, not discovered later. Contamination is a self-reversal:
the same authors dismissed it in 2022, then reversed later; tell it as a change
of view over time, not a steady critique. MMTEB adds Borda-count and a
category-weighted average on top of the mean; it does not remove the mean. The
"a model tops the average while being weak at retrieval" point is a property of
the averaging method, so frame it as a mechanism, not a named scandal. Background
links, not re-teaching: word-embeddings, retrieval.
