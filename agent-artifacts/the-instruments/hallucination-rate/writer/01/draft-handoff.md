# Draft handoff: the-instruments/hallucination-rate (01)

## Original work
This lesson runs a single fabricated-passage instance through Vectara's actual
scoring pipeline to show a false claim earning a clean "no hallucination" mark,
and sets the leaderboard's single-digit summarization rates against the same
model class's 33-48% PersonQA fabrication, demonstrating that the published rate
measures summary faithfulness rather than truthfulness. The evidence record
holds these as separate facts; the piece is what runs one document through the
pipeline until the seam is visible arithmetic and stages the cross-task
contrast.

## Proof result
`./nb check ... --series the-instruments --library <checkout>` (links included):
**BLOCK: 0**, verdict PUBLISHABLE. `nb stamp` written (words 2092, reading 9 min,
sources 9). All 9 source URLs resolved under `--check-links` (no link block).

Two W-SENTENCE-DENSITY warnings left intentionally:
- The dek (41 words, 2 joins). A dek is one designed newsstand sentence carrying
  the instrument's identity, its mechanism, and the misreading in a single line;
  splitting it would weaken the front-page card. Left as is.
- The "three things it cannot tell you" enumeration in the Why-this-matters
  bookend (40 words, 2 joins). It is a deliberate list of three that mirrors the
  three-part takeaway (the template's setup/resolution pairing); the editorial
  standard explicitly permits writing the list. Left as is.

## Mold-break note
Broke the series' paired-number / stacked-choices-table reflex: no headline pair
of conflicting figures, and no ranking table. The one worked seam (false-passage
trace), a Vectara-caveat note, a four-step pipeline, and a three-figure stat
strip (9.6% summarization vs 33%/48% PersonQA) carry the argument instead.

## Open evidence / voice questions
- The commission asked for a documented case where trusting the rate "cost"
  someone something concrete. Per the evidence record's flagged gap, no specific
  attributable dollar/procurement cost was found. The piece honors that: it
  frames the cost as misinformed buyers, policymakers, and public plus a false
  sense of reliability (Tom's Hardware ranking + NYT framing + the task-vs-open-
  use PersonQA gap), and invents no figure. If a hard cost anecdote is required,
  that remains a research gap owned by the researcher/orchestrator.
- Dates handled per brief: leaderboard rates attributed to the source's
  "May 11, 2026" snapshot; no overstatement of currency.
