# draft-handoff: the-instruments/hallucination-rate (writer/02)

## Editorial request resolved
- Required (repeated opener): rewrote the orientation opener from "Ask which chatbot
  invents the fewest facts and the answer usually arrives as one percentage" to
  "Hallucination percentages rarely reach the public as bare numbers. They arrive
  already read as scorecards for how often a model lies." The two openers no longer
  echo; the new opener supplies "Hallucination percentages" → "They" as the antecedent
  for the untouched "Most of those percentages start at a leaderboard..." sentence, so
  the orientation's antecedent chain holds. The editor's dek cut, signpost cut, and
  `nb stamp` are preserved; the claim set is unchanged.

## Proof result
`nb stamp` (words 2078, reading 9 min, sources 9), then full proof with links:
BLOCK: 0 — verdict PUBLISHABLE. Two W-SENTENCE-DENSITY warnings intentionally left:
both are pre-existing template-paired enumeration sentences ("three things it cannot
tell you" in Why-this-matters and its mirror in the takeaway) that the editor already
adjudicated and kept in editorial/01; neither arises from this revision.
