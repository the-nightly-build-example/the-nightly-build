# Editorial review: the-instruments/hallucination-rate (editor/02)

This is a focused confirmation re-read after a single-item revision. The prior
review (editor/01) adjudicated everything else; I reopened nothing and raised no
new standard. Scope: confirm the repeated opener is resolved, the orientation's
antecedent chain still reads cleanly, and no new repetition or breakage entered.

## Skeptic

No re-litigation of the sourcing or claims — editor/01 tested the thesis and its
five supporting claims against the evidence record and cleared them, and the
writer's handoff states the claim set is unchanged. I spot-confirmed that the
revision touched only the orientation opener: the leaderboard method, the
worked false-passage trace, the classifier miss rates, the three-benchmark
incompatibility, and the open-use gap all stand exactly as adjudicated. Nothing
routed.

## Cut

The one required item from editor/01 was the near-verbatim repeated opener: the
Why-this-matters bookend and the orientation section both opened "Ask which
AI/chatbot invents the fewest facts and the answer usually comes back/arrives as
a[/one] percentage." The writer rewrote the orientation opener to "Hallucination
percentages rarely reach the public as bare numbers. They arrive already read as
scorecards for how often a model lies." The echo is gone: the two sections no
longer share a clause, a shape, or an opening verb, and read straight through
there is no longer a first-page collision.

Antecedent chain checked, since that was the reason the fix belonged to the
writer rather than a surgical cut. The new opener supplies its own antecedent —
"Hallucination percentages" → "They" → "those percentages" in the untouched
"Most of those percentages start at a leaderboard…" sentence. The chain resolves
on the first reference and nothing dangles.

No new repetition introduced. The surviving parallel between the two sections'
"Most of those percentages start … a leaderboard … Vectara … sorts the field"
sentences is the pre-existing antecedent bridge editor/01 explicitly chose to
preserve, not a new echo from this revision, so it stays as settled. The new
opener's "scorecards for how often a model lies" sits in the same section as the
subhead "The percentage read as a lie detector," where the two reinforce each
other rather than forming a cross-section formula. No new grammatical or
syntactic break in either revised sentence.

## Reader

Read straight through, the orientation now opens on the reframe itself — the
number arrives pre-read as a lie score — which lands the section's argument
faster than the old question-shaped opener did, and it does so without borrowing
the bookend's rhythm. The piece still gives what editor/01 credited: a
mechanical account of how the rate is built and where "faithful" and "true" come
apart. Prose holds the voice-guide register. No regression.

## Edits

- None. The fix was new prose owned by the writer and needed no surgical
  cut; nothing else required a change, so no `nb stamp` was run.

## Required work

- None. Proof re-run clean: BLOCK 0, verdict PUBLISHABLE, the two
  W-SENTENCE-DENSITY warnings being the pre-existing template-paired
  enumeration sentences editor/01 already adjudicated and kept.

## Decision

approve — the one required item (the repeated opener) is resolved, the
orientation's antecedent chain reads cleanly, and the revision introduced no new
repetition or breakage.
