# Draft handoff: the-instruments/training-compute (01)

## Original work

The article turns the evidence's separate facts into one reversal: the FLOP
figure a lab produces to report how big a training run was is the same figure
regulation reads backward as a verdict on capability and danger, and it makes
that reversal concrete by setting the one disclosed frontier number (Llama 3.1
405B, Meta's own 6ND accounting) against the one that lives only as a third-party
estimate (GPT-4, Epoch's ~2.1x10^25) on the same EU line both straddle, so the
reader sees the frontier is drawn mostly on inferred markings.

## Proof

`nb check ... --series the-instruments --library /home/user/library-checkout`,
links included: **BLOCK: 0, WARN: 0, PUBLISHABLE.** 2186 words (band
1200-2200), 12 sources (9 primary, 3 secondary; series floor is 8 with >=4
primary, >=1 secondary). No warnings left standing.

## Legal-text URLs (for the editor)

The two owning legal texts are bot-gated to automated fetches and resolve
normally in a browser; the link check passed with them in place.

- Source 1 (EU AI Act) cites the canonical EUR-Lex URL
  (`eur-lex.europa.eu/eli/reg/2024/1689/oj/eng`), which returns HTTP 202 to
  automated fetches. The researcher verified the exact Article 51(2) and Recital
  111 wording against the Future of Life Institute verbatim reproduction, which
  matched the Official Journal text.
- Source 2 (EO 14110) cites the govinfo PDF (88 FR 75191), the official
  Federal Register text, which resolves cleanly; `federalregister.gov` itself
  redirects automated fetches to a proxy host.

Both are cited as primaries of record per the evidence; the mirrors were
verification aids only and are not cited.

## Notes on nuance handled

- "Almost never measured" is carried as the thesis and refined, not
  contradicted: Llama 3.1 405B is named as the disclosure counter-case, and
  even its disclosed 3.8x10^25 is framed as Meta's own 6ND-and-hardware
  accounting, not a metered quantity. Disclosure is the exception.
- The compute-threshold debate is steelmanned on both sides (Heim & Koessler
  for, Hooker against), and the law's own hedge is shown from inside the text:
  the EU 10^25 line is stated as a rebuttable presumption, with Recital 111
  quoted calling compute "one of the relevant approximations for model
  capabilities."
- EO 14110's status is stated plainly as revoked on 2025-01-20 by EO 14148; the
  10^26 / 10^23 / 10^20-per-second thresholds are given as the interim triggers
  they were.
- Compute-optimal training is not re-argued: Kaplan and Chinchilla appear as the
  numbered primary sources for the 6ND method, and the library lessons
  the-evidence/scaling-laws-kaplan and the-evidence/chinchilla are linked in
  prose (and in Background) rather than re-taught. cost-per-token,
  energy-per-query, and tokens-per-second are linked as the numbers this lesson
  is not about.

## Open questions

None blocking. One judgment call for the editor: the headline centers the GPT-4
case (the sharpest instance) rather than the general estimation lesson; the dek
carries the general finding. If the desk prefers the lesson-level claim in the
headline, the GPT-4 line can move to the dek, but the current split keeps a
named-actor finding up top per the headline standard.
