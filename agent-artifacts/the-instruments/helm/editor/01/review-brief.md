# editor review-brief: the-instruments/helm (01)

Inputs:
- .nb-work/the-instruments/helm/agent-artifacts/the-instruments/helm/editorial-direction.md — the standing standards.
- .nb-work/the-instruments/helm/agent-artifacts/the-instruments/helm/writing-coach/01/voice-guide.md — read first.
- .nb-work/the-instruments/helm/agent-artifacts/the-instruments/helm/commission.md — assignment, boundaries, recent-pattern notes.
- .nb-work/the-instruments/helm/agent-artifacts/the-instruments/helm/writer/01/brief.md — the exact writer brief (for leak detection).
- .nb-work/the-instruments/helm/agent-artifacts/the-instruments/helm/researcher/01/evidence.md — open when a concern calls for it.
- .nb-work/the-instruments/helm/agent-artifacts/the-instruments/helm/writer/01/draft-handoff.md — the writer's original-work sentence and open question.
- .nb-work/the-instruments/helm/library/the-instruments/helm.html — the drafted article (includes a captured Figure 26 asset).
- .nb-work/the-instruments/helm/.nb-context/ — the contract and furniture catalogs.

Output: .nb-work/the-instruments/helm/agent-artifacts/the-instruments/helm/editor/01/editorial-review.md

Proof: /home/user/the-nightly-build/nb check /home/user/the-nightly-build/.nb-work/the-instruments/helm/library/the-instruments/helm.html --series the-instruments --repo /home/user/the-nightly-build

Work from these inputs. Do not tour the repository or the archive. Where you need a fact nobody gathered, ask me (the orchestrator) and keep working.

Watch especially, on this piece:
- The correctness spine: the "same results reorder under other defensible choices" case must rest on the axes the record verified move the order (toxicity, bias, calibration) plus model-set/scenario/sample/aggregation-method choices — NOT on dropping robustness/fairness (which track accuracy). Confirm the piece states that accuracy/robustness/fairness are strongly correlated and that this is CRFM's own reason for dropping them in Lite. If any reorder claim rests on the wrong axis, that is a correctness fix.
- "Mean win rate" must match the record's verbatim definition. HELM's 2025 abandonment of it (model-set dependence, rank instability) is primary support — confirm it is represented, not overstated.
- The piece must not build any part on "a model judge prefers longer answers."
- The captured Figure 26 asset: confirm the crop shows what the caption/prose claims (six per-metric rankings; the model swap the prose names), the caption is factual and cited, and the alt text is useful. Recompute any figure/number in the prose against the record.
- The writer's open question (exact metric set feeding the Classic headline column, unread because JS-rendered) is non-blocking; confirm the prose does not assert which metrics enter that column.

Recent-record comparison (formula/leak detection): this desk overuses the "the number moves when the thing doesn't" instability mold (comet-score, brier-score) — HELM's point is aggregation hiding disagreement, so confirm the framing is aggregation, not instability. Flag any two-sentence "precise number then reversal" dek, any comma-triad or semicolon-reversal dek, and any "clause, and clause" heading.

Decide approve or redraft per the skill. Redraft only if the piece needs a different argument; everything else is yours to fix directly. After any direct edit, re-run nb stamp and the exact nb check above until BLOCK: 0, then write the review. Note whether you left the article at BLOCK: 0 and whether a writer round is owed.
