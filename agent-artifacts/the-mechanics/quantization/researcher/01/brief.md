# researcher brief: the-mechanics/quantization (01)

Inputs:
- ../../editorial-direction.md — citation standard, series territory, declared reader
- ../../commission.md — the behavior, the causal chain to confirm or break, and the boundary

Output: ./evidence.md

This round's focus: the measured cost of quantization at 8-bit and 4-bit
(perplexity or task-accuracy deltas, each against the primary that owns it), a
concrete memory-size drop for a named real model, and the outlier-feature
finding with the exact conditions under which naive low-bit rounding fails and
what the better schemes do about it. Distinguish what is settled from what is
still open at very low bit-widths. Record any measurement that contradicts the
"usually cheap" framing in full. Preserve a full degradation-versus-bits series
where one exists, in case a chart helps.
