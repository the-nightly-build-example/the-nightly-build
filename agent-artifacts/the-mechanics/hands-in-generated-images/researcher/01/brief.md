# researcher brief: the-mechanics/hands-in-generated-images (01)

Inputs:
- ../../editorial-direction.md — citation standard, series territory, declared reader
- ../../commission.md — the causal chain, boundaries, source-count policy

Output: evidence.md (in this directory)

This round's focus: establish each step of the causal chain against the primary
that owns it. From the diffusion literature: that generation optimizes for
plausibility with no explicit anatomy or counting step. For the hand-specific
step, find and read firsthand the strongest available primaries — research
studying or fixing generative hand/anatomy failures (e.g. hand-refinement or
pose-guided methods), and any analysis of why small, high-variance structures fail
— and record exactly what each establishes versus what is practitioner
explanation. Get any concrete figures (e.g. measured error rates on hands, or a
model builder's own stated numbers) with their owning source. Record the honest
line between what is settled and what is a moving target improved by scale and
data. Meet the policy: >=8 sources, >=4 primary, >=1 secondary. Resolve URLs to
each document's own page; classify primary/secondary by authorship and stake. If
the hand-specific evidence is thin, say so and name what the mechanism rests on.
