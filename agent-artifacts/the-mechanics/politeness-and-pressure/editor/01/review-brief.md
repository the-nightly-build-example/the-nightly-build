# review brief: the-mechanics/politeness-and-pressure (editor/01)

Inputs (read in the order the editor skill names):
- `editorial-direction.md` (artifact root) — house standard, press voice, series prompt.
- `writing-coach/01/voice-guide.md` — how this piece should sound (read first).
- `commission.md` (artifact root) — the assignment, boundaries, and the reader's situation.
- `writer/01/brief.md` — the exact writer brief (so a leak is visible against it).
- `researcher/01/evidence.md` — the evidence record (leave closed until the skeptic read calls for it).
- `writer/01/draft-handoff.md` — the writer's handoff (original-work sentence closed until the third read).
- The article: `.nb-work/the-mechanics/politeness-and-pressure/library/the-mechanics/politeness-and-pressure.html`.
- Template context under `.nb-work/the-mechanics/politeness-and-pressure/.nb-context/`.

Output: `.nb-work/the-mechanics/politeness-and-pressure/agent-artifacts/the-mechanics/politeness-and-pressure/editor/01/editorial-review.md`

Round focus:
- This is a mechanism lesson that must mark each step as settled engineering or open
  question (the series requires it). Verify the piece draws that line honestly: the
  settled half (output conditions on all prompt text; tone-sensitivity is a trained
  disposition) versus the open half (no specific trick reliably improves modern-model
  answers). Push on any claim that overstates the payoff.
- The evidence record flags some exact decimals as summary-sourced/approximate and
  others as confirmed (e.g. Yin GPT-3.5 MMLU 60.02/59.44/51.93). Check that no
  unverified decimal is presented as exact, and that the EmotionPrompt/Bsharat
  steelman is weighed as older/smaller-model evidence rather than dropped or
  overstated.
- No code belongs in this article; confirm none crept in.

Recent-pattern notes (compare edges, headings, dek, and furniture against these):
- House tics forming across the paper, cut on sight if present: the why-bookend closer
  "By the end you will/can..."; the device "It is tempting to say X. That goes too
  far."; the phrase "doing the work" ("X, not Y, is doing the work"); the
  negative-parallelism reflex ("not A, it is B") where the "not" clause is an invented
  strawman.
- The last three Mechanics pieces were text-in-images, first-token-latency,
  false-confidence. text-in-images opened "You [do a small thing], and [the failure].
  This is the one failure everyone notices..." and closed its takeaway on a posed
  diagnostic question ("Is the model failing to X, or failing to Y?"). If this draft's
  opener or takeaway is built to either shape, it is a formula — break it without
  copying prior structure.
