# editor review-brief: the-mechanics/formatting-defaults (01)

Inputs (under the article's agent-artifacts root unless noted):
- `editorial-direction.md` — house standard, paper voice, lesson identity, series prompt.
- `commission.md` — the assignment, boundaries, and the reader's situation.
- `writer/01/brief.md` — the exact writer brief (check for leakage against it).
- `writing-coach/01/voice-guide.md` — read first; compare distinctive phrasing against its quoted passages.
- `researcher/01/evidence.md` — the claim set the article may draw on.
- `writer/01/draft-handoff.md` — proof result and original-work sentence (open at the third read).
- Article: `.nb-work/the-mechanics/formatting-defaults/library/the-mechanics/formatting-defaults.html`, with `chart-1.py`/`chart-1.png` in the sibling slug directory.
- Template context: `.nb-work/the-mechanics/formatting-defaults/.nb-context/`.

Output: `.nb-work/the-mechanics/formatting-defaults/agent-artifacts/the-mechanics/formatting-defaults/editor/01/editorial-review.md`

Round focus:
- The lesson is a backward walk (orientation, tokens, imitation, preference, ground) that marks each link settled or open, per the mechanics desk requirement. Check every step names a real part and what it does, that the settled/open marking is honest, and that the chain reaches ground (a learned, steerable output policy).
- Verify the load-bearing precision: structure-and-length together with length the heavier half (LMSYS coefficients length ~0.249 vs ~0.019–0.031 per markdown element), phrased "substantially independent," not "wholly content-free," with LMSYS's observational caveat intact. Human-vote and automated-judge evidence must be attributed separately.
- The proof left one intentional warning: W-CITE-DENSITY on the `tokens` section. The evidence record marks the pretraining/token contribution as open and unmeasured, so it is kept as a step without a padded citation. Confirm this is the right call and the prose says the size is unmeasured; do not push the writer to invent a citation there.
- Confirm the piece differentiates from the-mechanics/prompt-sensitivity (input formatting changing accuracy) rather than echoing it, and that no code appears.
- Inspect the chart provenance against the evidence record and read the image (labels, scale, the length-vs-markdown coefficients).

Recent-pattern notes for this desk (the-mechanics):
- Avoid the recurring "X is all the system ever does" opener/closer (image-generation) and the single-mechanism "the behavior is really just Y" reveal — this behavior has several stages, so flag any sentence that flattens it to one cause.
- Recent deks state a mechanism as one plain surprising fact; check the dek is built in this piece's nouns and headings vary in construction and read as argument steps.
