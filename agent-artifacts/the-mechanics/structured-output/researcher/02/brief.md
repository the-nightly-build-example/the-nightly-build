# researcher brief: the-mechanics/structured-output (02)

Inputs:
- researcher/01/evidence.md — the prior evidence record to carry forward

Output: evidence.md (this directory)

Single-owner follow-up. The round-01 record cited 8 sources all classified
primary, and the series floor requires at least 1 secondary; the gated OpenAI
announcement also left the total fragile if dropped. Add at least one genuine
secondary source (context on the constrained-decoding / format-restriction
debate), enough that the record clears 8 total with >=1 secondary after any single
drop, and resolve the gated OpenAI announcement: confirm its mechanism through an
openable source or mark its exact eval figures not-to-be-quoted. Keep the
"JSON mode" vs "structured outputs" terminology-collision note prominent.
