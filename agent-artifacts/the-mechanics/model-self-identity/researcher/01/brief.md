# researcher brief: the-mechanics/model-self-identity (01)

Inputs:
  ../../editorial-direction.md — citation standard, series territory, declared reader
  ../../commission.md — the behavior, source floor, distinctness lines

Output: ./evidence.md

This round's focus:
  - A verified, dated case of the behavior: which model identified itself as
    ChatGPT/OpenAI (or another wrong maker), when, and exactly what was asked and
    answered. Record it as a reproduction with model and date; if you can
    reproduce it yourself, say so; if you rely on a report, get an independent
    confirmation or the developer's own acknowledgement.
  - The identity-injection mechanism from primary developer docs: a model spec,
    system prompt, or model card that shows the assistant's name and maker are set
    by system prompt and/or post-training, not discovered by the model.
  - The base-distribution / distillation cause: a primary on synthetic-data or
    distillation contamination, and (if findable) a primary on LLM
    self-knowledge/introspection and its limits. Keep "web contamination" and
    "trained on another model's outputs" separate, and mark which, if either, is
    verifiable for the case you use.
  - Contradictions in full: any developer denial, or evidence that a self-report
    did reflect provenance.
Verify every claim against the primary that owns it; confirm URLs resolve to the
document's own page. If the min-8 / primary-4 floor cannot be met with verifiable
documents, say exactly what is missing rather than padding.
