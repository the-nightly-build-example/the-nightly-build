# Draft handoff: the-mechanics/overthinking (01)

## Original work

The article turns the evidence record's separate warnings-against-conflation
into a single backward causal walk that pins each of three distinct "more
tokens" failures to its own mechanism (wasteful repetition on easy inputs,
amplified error on constructed traps, an abandoned correct line on hard
problems) and marks exactly where the settled length-reward engineering stops
and the open three-way framing dispute begins. The separation is made visible in
the regime-comparison table and the closing settled-vs-open section; the evidence
record supplies the findings but performs neither the walk nor the separation.

## Proof result

`nb stamp` then `nb check ... --series the-mechanics --library
/home/user/library-checkout` (links included): **BLOCK: 0, WARN: 0, verdict
PUBLISHABLE.** Stamped words=2192, reading_minutes=10, sources=8 (7 primary, 1
secondary; series floor met). No warnings left standing.

Display-text self-test done: headline figures (901 tokens, 2 plus 3) and the
subhead "Thirteen solutions" are owned by source 1 (Chen et al.); nb-meta dek is
identical to the rendered dekline; zero em-dashes; no banned terms.

Accuracy guardrails from the brief are held: trivial-input overthinking is
framed only as an efficiency cost (first round correct >92%, mitigation halves
tokens while accuracy moves 92.8% to 93.2%), never as a wrong answer; the
accuracy drops are stated as conditional, appearing on the constructed
distractor/spurious-feature/constraint tasks (Opus 4 ~100% to 85-90%; R1 ~70% to
30%) while standard benchmarks are maintained; the underthinking flip is carried
as a second mechanism; and the two cause attributions are kept distinct (RL
length reward for the wasted compute; amplified flawed heuristics, a cause
separate from the length reward, for the accuracy drops). thinking-out-loud is a
prose link, not a numbered source; no safety/monitoring drift.

## Open question

None blocking. One judgment for the editor: source 4 (VentureBeat) is the lone
secondary and is used once, only to show how the inverse-scaling finding was
framed for a general audience ("why thinking longer makes models dumber"); all
figures come from the owning primaries. If the editor prefers the secondary
carry more context or be swapped, that is the place to look. The evidence record
supplied every fact the piece needed; no researcher request is outstanding.
