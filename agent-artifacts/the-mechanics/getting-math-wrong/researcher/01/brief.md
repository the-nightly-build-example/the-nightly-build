# researcher brief: the-mechanics/getting-math-wrong (01)

Inputs:
- The commission at ../../commission.md — the angle, the causal chain, the source leads, the floor, and the boundaries.
- The editorial direction at ../../editorial-direction.md — the citation standard, the series territory, the declared reader.

Output: ./evidence.md

Focus: get firsthand a study of LLM arithmetic accuracy vs digit length (accuracy falling as digits grow, with the denominator), a tokenization source owning the "numbers split into chunks, not clean digits" claim (including single-digit tokenization in newer models), and an interpretability paper on how a transformer actually implements addition (distinguish modular-toy results from real multi-digit arithmetic — do not overclaim). Get a source showing chain-of-thought/scratchpad or a calculator tool fixes it. For each step of the commission's chain, record what supports it and mark settled vs open. If newer models have largely solved multi-digit arithmetic, record that and scope the claim. Classify each source; record contradictions. Meet the floor (>=8 sources, >=4 primary, >=1 secondary).
