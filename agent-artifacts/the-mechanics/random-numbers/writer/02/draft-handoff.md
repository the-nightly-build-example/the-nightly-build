# Writer handoff: the-mechanics/random-numbers (02) — revision

## Original-work statement (one sentence)

Still holds after the revision: the article assembles the separate measurements
and the raw 1-to-100 count table into a single step-by-step trace from the
observed "7" down to the token-probability-list-plus-sampler that produces it,
then to the human-learned and alignment-sharpened shape of that list, and it
renders the exmergo series as a uniform-referenced chart so the reader sees the
choice is biased rather than merely variable — synthesis and visualization the
evidence record does not itself perform.

## Editorial items resolved (one line each)

- **Required (writer): human-"69" claim.** Took option (a) — cut the 69 anomaly
  entirely. researcher/02 confirms no peer-reviewed or verifiable-with-a-figure
  human source supports "humans over-pick 69"; the only human signal is
  meme-contaminated crowdsourced polls (r/dataisbeautiful n=6,750; Veritasium),
  a different mechanism from the spontaneity bias that drives 7, so leaning the
  inherited-shape argument on it would be misleading even when hedged. Recast the
  "two findings" paragraph in the human-shape section to the one surviving finding
  (West & Potts: alignment amplifies the bias; base 13.9 vs aligned 52.3-129.1),
  which carries the "inherited then reworked by alignment" conclusion on its own,
  and kept the "how much is inherited vs added is not settled" open marker. The
  model-side under-pick (exmergo, 69 = 29 of 10,000) was dropped with it, because
  its "runs the wrong way" hook depends on the human premise; no source was
  orphaned (exmergo #s2 still cited three times elsewhere).
- **Required (writer): closing 69 reference.** Reconciled — removed the orphaned
  "or why 69 is pushed down while 47 is left alone" clause; the closing now poses
  the still-open question purely as "the exact path from the training text to the
  height of the bar over 47." No "69" string remains anywhere in the article.
- **Required (writer): Zhao/Du/Wang quotation.** Retrieved the published source
  (arXiv:2601.05414v3) and verified against two independent fetches. The printed
  quote "lack a functional internal mechanism for probabilistic sampling" is
  verbatim in NEITHER location — it is a stitched hybrid. The abstract reads
  "lack a functional internal sampler"; the Discussion reads "do not possess a
  genuine internal mechanism for probabilistic sampling." Replaced the article's
  phrase with the abstract's exact wording, current models "lack a functional
  internal sampler," which fits the existing sentence and matches the #s7 locator
  (Abstract; Appendix A, the latter for the temperature-ablation claim in the same
  sentence). Quotation now exact.

Editor's own direct edits (the two negative-parallelism trims, the forward-pass
jargon fix, the split semicolon, the recast "why 69 is pushed down" closing) were
preserved; the closing edit is superseded only where the 69 clause itself was cut.

## Proof result

- Command: `./nb check .nb-work/the-mechanics/random-numbers/library/the-mechanics/random-numbers.html --series the-mechanics --library /home/user/library-checkout`
- Full proof with links: **BLOCK: 0, WARN: 0, verdict PUBLISHABLE.**
- Re-stamped: 2093 words (lesson band 1200-2200), 8 sources (7 primary, 1
  secondary), reading 9 min. No warnings intentionally left.

## Open questions

None blocking. The claim set was not expanded: the r/dataisbeautiful crowdsourced
survey supplied in researcher/02 was available as a hedged support for the 69
human claim (option b), but I judged option (a) cleaner and more honest given the
meme contamination, so no new source was added. If a future editor wants the 69
model-side dip restored as a pure measured curiosity (no human claim), it can go
back into the closing on exmergo #s2 alone without disturbing the argument.
