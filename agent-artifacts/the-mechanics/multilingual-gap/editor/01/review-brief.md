# editor review-brief: the-mechanics/multilingual-gap (01)

Inputs (all under this article's artifact root unless absolute):

- `editorial-direction.md` — house standard, slop, headline standard, press voice,
  lesson identity, series prompt.
- `commission.md` — the assignment, boundaries, required contribution.
- `writer/01/brief.md` — the exact writer brief (check for leakage against it).
- `writing-coach/01/voice-guide.md` — register and quoted exemplars to check
  borrowed phrasing against.
- `researcher/01/evidence.md` — the evidence record; read for what breaks a claim.
- `writer/01/draft-handoff.md` — the original-work sentence (third read only).
- Article: `/home/user/the-nightly-build/.nb-work/the-mechanics/multilingual-gap/library/the-mechanics/multilingual-gap.html`
- Template context: `/home/user/the-nightly-build/.nb-work/the-mechanics/multilingual-gap/.nb-context/`

My recent-pattern notes (compare edges, deks, headings against these; a match is a
formula to break):

- Paper-wide opener tic: Why-this-matters built as "By the end you will know X.
  You will also see Y."
- The Mechanics opener tic: "the thing that feels like X is not what happens."
- The Mechanics closer tic: a stock "where this lives now" / "what the system never
  learned" heading.
- Takeaway on negative parallelism ("It is X, not Y"). Deks: banned molds
  (semicolon reversal, suspended question, comma triad); headings: comma-and triad.

This round's focus:

- The lesson must lead with training-data distribution as the primary cause and
  keep the token tax as the secondary amplifier. Check the ordering is earned, not
  asserted.
- Four caveats must survive and be marked settled vs open: token disparity not
  always a data artifact; the performance benchmark is machine-translated (part of
  the gap is translation quality); the gap narrows but does not close; the token
  premium is a tokenizer-training property, not a script property (MuRIL 1.21x
  Telugu). A draft that tells a clean single-cause story needs revision.
- The 93%-English figure is GPT-3 (2020), a dated proxy; confirm it is attributed
  to GPT-3 and paired with the Common Crawl raw-web share, not implied to be a
  current frontier mix.
- There is one chart (`chart-1.png` from committed `chart-1.py`, the UDHR token
  series). Inspect its committed provenance and read the image as a reader: axes,
  scale, and caption honest, numbers matching the evidence. Chart corrections go to
  the writer.

The orchestrator stamps after your direct edits and proves before the PR; return
to the writer only if the proof would demand new prose or a chart fix.
