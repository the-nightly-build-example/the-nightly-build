# writing-coach brief: the-mechanics/retrieval (01)

Inputs:
  ../../commission.md — behavior (grounded answer from an untrained document), angle, boundaries, sources plan
  ../../editorial-direction.md — house + headline standard, press voice, lesson identity, series prompt
Output: ./voice-guide.md

Round focus: the genre is explaining a system's mechanism by working backward
from a behavior the reader has seen — study exemplars who make an engineering
pipeline legible in plain words with a small worked example, no code. Compile
the "Recently used, do not reuse" list from the recent library's deks and
headings (`nb history --series the-mechanics`); the commission flags the stacked
short subject-verb heading cadence to vary.

Run-environment caveat: published library at `/home/user/library-checkout` for
`nb history`.
