# Brief 01 — writing-coach — the-mechanics/instructions-are-data

## Begin with these inputs only
- `agent-artifacts/the-mechanics/instructions-are-data/editorial-direction.md`
- `agent-artifacts/the-mechanics/instructions-are-data/commission.md`

Do not tour the repository or prior articles for voice. Use the web.

## Task
Follow the **writing-coach** skill (Skill tool: `writing-coach`). Study the best
writers who **explain how a system works by reasoning backward from a familiar
behavior to its cause** — technical explainers who make an architecture intuitive
without code, and security writers who explain a class of vulnerability plainly to
non-specialists. Find ≥3 exemplars the field rates (prefer primary explanatory
pieces); extract transferable craft: how they use a concrete example to expose a
mechanism, how they separate "settled" from "open," how they avoid mystifying or
over-dramatizing. No persona, no reusable lines.

The piece is a **qualitative** Mechanics **lesson** in *The AI Reader* (no
manufactured statistic, no code): plain, concrete, teach don't summarize; smart
reader, no ML background.

## Output (write only this)
`agent-artifacts/the-mechanics/instructions-are-data/writing-coach/01/voice-guide.md`
— concise directive first (register, reader relationship, the sentence-changing
moves, especially: using one assembled-prompt example to carry the point, and
marking settled vs. open honestly), ending with `Recently used, do not reuse:` from
the commission; then one block per exemplar. Return `DONE writing-coach <path>`.
