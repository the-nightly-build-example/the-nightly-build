# editor review-brief: the-mechanics/illegal-chess-moves (01)

Inputs:
- `editorial-direction.md` — the standard, voice, and series direction
- `commission.md` — the assignment, boundary, and the reader's situation
- `writer/01/brief.md` — the exact brief the writer worked from
- `writing-coach/01/voice-guide.md` — how this piece should sound, with exemplars
- `researcher/01/evidence.md` — the evidence record; read it against the claims,
  and read its Contradictions section closely
- `writer/01/draft-handoff.md` — the original-work sentence and the editor flag
- the article at
  `.nb-work/the-mechanics/illegal-chess-moves/library/the-mechanics/illegal-chess-moves.html`
- template context under `.nb-work/the-mechanics/illegal-chess-moves/.nb-context/`

Output: `editor/01/editorial-review.md`

This round's focus: the honesty of the two rebuilt framings. Confirm "no board" is
written as no external board the model checks (settled), that the internal-
representation section is scoped to small transcript-trained models (Othello-GPT,
Chess-GPT) and marked open for large chat models, and that compounding is taught as
the weak/chat-model failure mode and explicitly refuted for gpt-3.5-turbo-instruct.
Verify each step is marked settled or open, and that the engine (AlphaZero /
Stockfish) stays contrast, not subject. The writer flagged a specific display
figure: David Barry called "1800-rated" (his FIDE rating) while the cited result is
a blitz match where his rating is 1900. Open the source and confirm the label is
accurate and unambiguous as printed; fix or route if it misleads.

My recent-pattern notes (compare edges, dek, and headings against these; this
series' last eight lessons):

- The headline mold "Ask <system> for <X> and it <does Y>" (clock-faces,
  random-numbers) and the line "a model has no <X> to reach for" (random-numbers).
  The "an image generator <does X>" dek shape. Catch any of these as formula.

The lesson template allows only the two bookend cards to address the reader; hold
every other sentence to the no-self-reference rule.
