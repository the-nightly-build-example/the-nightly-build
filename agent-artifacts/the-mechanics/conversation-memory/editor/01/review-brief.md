# editor review-brief: the-mechanics/conversation-memory (01)

Inputs (read in the order your skill names):
- ../../writing-coach/01/voice-guide.md — read first.
- ../../editorial-direction.md
- ../../commission.md
- ../../writer/01/brief.md — note its "Orchestrator correction" paragraph on which
  neighbor pages are published; those links are valid.
- ../../researcher/01/evidence.md — open when the first read calls for it. Its
  course-placement note (only word-order published) is WRONG, per the writer
  brief's correction; do not act on it.
- ../../writer/01/draft-handoff.md — open only on the third read.
- ../../../../library/the-mechanics/conversation-memory.html — edit in place.
- template context under ../../../../.nb-context/.

## Recent-pattern notes (catch formula against these)

Cross-series house formulas this run is breaking — flag any that survived:
1. Opener closing on a "By the end you will be able to..." promise.
2. Takeaway landing on a second-person "next time... ask [N] questions" checklist.
3. Final move being a "settled-vs-open / demonstrated-vs-unproven" sort or a
   "which of the two you are looking at" line.
4. "this desk" self-reference.

the-mechanics-specific molds to check against — and one is critical: the verbatim
catchphrase "The mechanism is settled. What is not settled is..." appears in two
of the last three the-mechanics pieces and MUST NOT appear here in any form as a
closing beat. Recent openers used a second-person twin-example contrast ("'The dog
bit the man' and 'The man bit the dog'..."); recent deks used a two-clause
"..., and [the catch]" mold; headings were full declarative sentences with a
"...too" additive tail; recurring "a bag of words." Flag any of these that survived.

## Round focus

Verify the mechanism chain is taught correctly and completely: statelessness of a
single call and the within-request KV cache; the application resending the full
transcript each turn; and the consequences (new chat blank, context-window
overflow drops/summarizes the oldest turns, product "memory" injects stored facts
into the prompt). Each step marked settled engineering or product-dependent. The
claim "memory features store facts outside the weights" must read as reasoning
from the cited pages, not as a quotation; the product-memory citation should be
the Anthropic memory-tool page, not the gated OpenAI FAQ. The neighbor Background
links (prefill-and-decode, knowledge-cutoff, retrieval, the-instruments/context-window)
are published and valid. Audit every data-nb-kind. Three ordered reads; edit
directly; route only reporting you lack; record every change.
