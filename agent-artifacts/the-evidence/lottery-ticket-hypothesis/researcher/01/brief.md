# researcher brief: the-evidence/lottery-ticket-hypothesis (01)

Inputs:

- `commission.md` (at the artifact root) — the document, angle, source policy,
  neighbors, and what the piece must establish.
- `editorial-direction.md` (at the artifact root) — the citation standard, series
  territory, and declared reader.

Output: `researcher/01/evidence.md` (under this article's artifact root).

Answer these specific questions from the primary documents:

- The exact statement of the lottery ticket hypothesis in Frankle & Carbin 2019,
  and the iterative magnitude pruning procedure step by step, including that the
  survivors are reset to their original initial weights (and that a random
  reinitialization does not match). Quote and locate.
- The scale of the central result: which networks and datasets (MNIST, CIFAR-10,
  the specific architectures), the sparsity levels reached, and the accuracy
  matched. Give the figures with the Numbers shape.
- The existence-versus-cheap-finding point: that winning tickets are identified
  by pruning after training, not found cheaply up front. Pin to the paper.
- How the result held up: Frankle et al. 2020 ("Linear Mode Connectivity and the
  Lottery Ticket Hypothesis") and the move to rewinding to an early-training
  iteration for larger networks; and the critical/replication literature (Liu et
  al. "Rethinking the Value of Network Pruning"; Zhou et al. "Deconstructing
  Lottery Tickets"). Establish what each confirmed, corrected, or complicated.
- A concrete present-day usage of the "winning ticket" idea, so the present-day
  gap is reported, not asserted.
- Contradictions: search for what breaks the commission's angle; record in full
  both the results that limit the original method (failure to scale without
  rewinding, no cheap up-front ticket) and any that strengthen it.

Meet the source policy in the commission with sources that change the
interpretation. Every URL must resolve to the document's own page.
