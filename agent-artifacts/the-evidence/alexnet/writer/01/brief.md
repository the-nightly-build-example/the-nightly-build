# writer brief: the-evidence/alexnet (01)

Inputs:
  ../../commission.md — the document, angle, boundaries, sources plan
  ../../editorial-direction.md — house + headline standard, press voice, lesson identity, series prompt
  ../../writing-coach/01/voice-guide.md — register (credit-then-correct), licenses, do-not-reuse list
  ../../researcher/01/evidence.md — the complete claim set; use its Numbers and Sources exactly
  ../../../../library/the-evidence/alexnet.html — the initialized article to edit (do not recreate the skeleton)
  ../../../../.nb-context/ — effective template contract, runtime assets, furniture catalogs
Output: ./draft-handoff.md
Proof (links included, until BLOCK: 0):
  ./nb check .nb-work/the-evidence/alexnet/library/the-evidence/alexnet.html --series the-evidence --library /home/user/library-checkout

Round focus: two evidence findings sharpen the through-line — use them precisely.
(1) The famous 15.3% top-5 is the seven-CNN ensemble (two nets pre-trained on
extra Fall-2011 data); the single model the paper describes scored 18.2% top-5,
still ~8 points ahead of the 26.2% runner-up. Do not let "15.3%" stand in for
the single model. (2) GPU-trained deep CNNs (Ciresan/DanNet) were already winning
vision contests before AlexNet, and AlexNet cites that work — this supports the
"one result on preconditions" line, not an invention story. The "superseded"
case has three separate owners (BatchNorm retiring LRN; ViT challenging the
convolutional form; Recht et al. on generalization) — attribute each to its own
primary; overstate none as a universal verdict. Per the coach: credit the real
~10-point win fully in the same passage that corrects the shorthand; keep the
dek off the negative-reveal mold the desk has overused.
