# Draft handoff: the-evidence/flashattention (writer 01)

## Original work

The evidence record hands over the paper's numbers as separate, itemized
facts (four differently-scoped speedup figures, a memory-scaling table, an
exactness quote, a flagged sourcing asymmetry on the SRAM bandwidth figure);
this draft's original work is building those facts into one argument, a
worked table size that motivates a two-tier memory picture, which motivates
the tiling mechanism, which is then measured by the honestly-separated
speedup numbers and the memory-scaling chart, closing on an earned
correction of the "fewer FLOPs" misreading grounded in the same GFLOPs
figures introduced earlier — while keeping the third-party SRAM-bandwidth
estimate visibly distinct from the vendor-sourced HBM figures throughout,
rather than letting the two blend into one "vendor spec" impression.

## Proof result

Final run: `./nb check .nb-work/the-evidence/flashattention/library/the-evidence/flashattention.html --series the-evidence --library /tmp/claude-0/-home-user-the-nightly-build/d5bad8fd-c10f-5854-a93d-2fb67333d30d/scratchpad/library`
(links included) → **BLOCK: 0, WARN: 0**. `nb stamp` reports words=2200 (top
of the lesson band, 1200–2200 — intentional; the piece needed the full
budget to keep the four scoped speedup numbers, the memory chart, and the
present-day succession each argued rather than asserted, and every trim
pass past this point started cutting numbers or locators instead of
padding).

No warnings were left unaddressed; the run that reached BLOCK: 0 also
reported WARN: 0.

## Open questions

None. The evidence record's Contradictions and Numbers sections fully
settled this round's focus (the scoped speedup figures and the SRAM
bandwidth's third-party sourcing), and no claim needed here was missing
from the record.
