# Draft handoff: the-evidence/gpt-2 (writer 01)

## Original-work statement

The article pulls GPT-2's measured result apart from its fame by putting the
paper's actual zero-shot numbers in the reader's hands first (records on
language-modeling benchmarks, weak-to-rudimentary on the generative downstream
tasks, competitive only on CoQA and Winograd) and only then showing that the
"too dangerous" framing lived entirely in OpenAI's blog posts and follow-up
reports, so the reader sees the danger story as being about a different quality
of the model (its fluency) than the benchmarks ever measured, rather than one
undifferentiated legend.

## Proof result

Final `nb check` (links included, after `nb stamp`): `BLOCK: 0`, `WARN: 0`,
verdict PUBLISHABLE. Stamped words=2165, reading_minutes=9, sources=7 (3 primary:
GPT-2 paper, Nov 2019 report, Aug 2019 report; 4 secondary: Wikipedia, Slate,
TechCrunch, The Decoder).

No warning was intentionally left. The link check surfaced no egress warnings in
this run even for the openai.com CDN sources, so nothing needed to be recorded as
standing.

## Open evidence/voice question

One item is unresolved at the evidence level, not fixable by the writer: the
February 14, 2019 OpenAI announcement blog is egress-blocked, so its exact line
("Due to our concerns about malicious applications of the technology, we are not
releasing the trained model") is carried only through Wikipedia. The draft
attributes it precisely as "quoted in Wikipedia" and cites source 2 rather than
the blog. If a later pass regains access to the primary blog page, that quote and
the "concerns about malicious applications" phrasing should be re-cited to the
OpenAI page directly.
