# researcher brief: the-evidence/constitutional-ai (02)

Inputs:
- editorial-direction.md (../../editorial-direction.md)
- the prior evidence record (../01/evidence.md) — preserve all still-valid work
- the editor review (../../editor/01/editorial-review.md) — the finding to fix

Output: evidence.md (this directory) — a complete new evidence record preserving
still-valid 01 work with the one correction below.

Narrow correction only. The 01 record's TIME entry (source s6) records the phrase
"addressed to Claude and used at different stages in the model's training to shape
its character" as a quotation from Amanda Askell. The editor confirmed (two fetches
of the TIME page) that this phrase carries NO quotation marks and NO Askell
attribution in the source — it is the reporters' own narration. Fix exactly this:

- Reopen the TIME page firsthand and confirm the phrase is the reporters'
  description, not an Askell quote. Correct the s6 entry so the record attributes
  the wording to the TIME reporting itself, not to Amanda Askell, and does not
  present it as quoted speech.
- Check the rest of the s6 entry for any other wording that is narration
  mislabeled as a quote, and fix if present.

Everything else in the 01 record verified clean by the editor (directional results,
the three tensions, the two verbatim constitution principles, the "ad hoc"
admission, the RLHF baseline, the figure asset). Preserve it unchanged. Keep the
source floor (6 sources, >=3 primary, >=1 secondary). Confirm every URL resolves to
the source's own page. Report the corrected attribution in your report.
