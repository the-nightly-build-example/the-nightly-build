# draft handoff: the-evidence/constitutional-ai (02)

## Editor item resolved

Resolved the editor/01 required-work writer item: in the present-day section,
the sentence that read "Reporting on the update quotes Anthropic's Amanda
Askell describing the constitution as text 'addressed to Claude and used at
different stages in the model's training to shape its character'" was
rewritten to attribute the wording to the TIME reporting itself (source s6)
instead of to Askell, with the quotation marks removed so it reads as reported
description rather than quoted speech. The wording and the s6 citation are
unchanged; nothing else in the article was touched.

## Proof result

`nb check .../constitutional-ai.html --series the-evidence --library
/home/user/library-checkout` (links included): **BLOCK: 0, WARN: 0,
verdict: PUBLISHABLE**. Same result with `--no-check-links` during iteration
(the sentence-level fix needed no further iteration). `nb stamp` last run:
words=2182, reading_minutes=9, sources=6.

No warnings were intentionally left; the proof is clean.
