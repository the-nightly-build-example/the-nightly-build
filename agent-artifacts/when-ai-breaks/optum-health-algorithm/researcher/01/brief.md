# researcher brief: when-ai-breaks/optum-health-algorithm (01)

Inputs:
- /home/user/the-nightly-build/.nb-work/when-ai-breaks/optum-health-algorithm/agent-artifacts/when-ai-breaks/optum-health-algorithm/editorial-direction.md — citation standard, series territory, declared reader
- /home/user/the-nightly-build/.nb-work/when-ai-breaks/optum-health-algorithm/agent-artifacts/when-ai-breaks/optum-health-algorithm/commission.md — subject, angle, required contribution, source policy
- this brief

Output: /home/user/the-nightly-build/.nb-work/when-ai-breaks/optum-health-algorithm/agent-artifacts/when-ai-breaks/optum-health-algorithm/researcher/01/evidence.md

Read the Obermeyer, Powers, Vogeli, Mullainathan Science 2019 paper firsthand
("Dissecting racial bias in an algorithm used to manage the health of
populations"). If Science is gated, reach the full text via the authors' hosted
copies (Berkeley/Booth) or the NBER/institutional version, and record the paper's
own page as the URL. Pull the exact numbers: the study population and denominators
(the ~50,000-patient analytic sample and any larger frame); the risk-score
comparison (extra chronic conditions for Black vs white patients at the same score);
the counterfactual enrollment share (the reported jump in the Black share of the
high-risk group under an unbiased algorithm, e.g. from ~17-18% to ~46-47% - confirm
the exact figures); the ~200M-people scale figure for such tools; and the fact that
race was not a model input. Record the mechanism in the paper's own terms (cost as
the label / proxy for need). For aftermath, find primary or authoritative statements
of the New York DFS/DoH inquiry and the vendor's response/engagement. For "where it
lives now," find one documented present-day proxy-label case from a primary. Get
every name, title, and affiliation exact. Every URL must resolve to the document's
own page. Source policy: min 8, primary >= 4, secondary >= 1. Note any figure the
paper gives as an estimate, and any detail popular retellings get wrong (e.g.
naming a specific vendor the paper deliberately did not name).

### Source asset
The paper's key figure (risk score vs number of chronic conditions by race, or the
cost-vs-illness panels) is candidate visual evidence. If one would let the reader
test the argument, record it in Source assets with what a crop must retain;
otherwise write `None found`.
