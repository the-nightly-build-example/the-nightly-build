# Writer handoff: when-ai-breaks/bard-jwst-demo (01)

## Original work

The evidence record is an atomized set of sourced facts, quotes, and flagged
contradictions. This draft turns that record into a single dated argument: it
sequences the launch, the correction, and the market reaction into one
throughline; it converts the evidence's "cause of the share fall" contradiction
into explicit, reasoned prose that separates reported co-occurrence from
causation rather than leaving it as a caveat; it uses the incident's own
specifics (the exact false sentence, the exact 2004/2005/2022 imaging record) to
make the taught mechanism (fluent generation with no built-in truth check, and
confidence that is not a calibration reading) concrete rather than restating it
abstractly; and it closes by carrying that same mechanism forward into a named
2024 product failure (AI Overviews) so the reader leaves able to spot the
pattern outside this one incident.

## Proof result

Final command: `nb check
/home/user/the-nightly-build/.nb-work/when-ai-breaks/bard-jwst-demo/library/when-ai-breaks/bard-jwst-demo.html
--series when-ai-breaks --library /home/user/library-checkout` (links checked)

```
BLOCK: 0
WARN:  0
verdict: PUBLISHABLE
```

`nb stamp` was run beforehand: words=1818, reading_minutes=8, sources=8.

No warnings were intentionally left; the proof is clean at BLOCK 0 / WARN 0.

## Notes

- The three primary originals the researcher could not fetch firsthand (the
  Pichai tweet; Reuters, The Verge, and CNN at their own domains) are not cited
  directly. The verbatim Bard sentence and Google's statement are cited to the
  outlets the evidence record actually captured them from (Engadget; the
  Reuters wire story as it resolves at Al Jazeera), per the brief's sourcing
  note.
- I confirmed the exact published headlines for all eight sources (used as the
  works within the Sources list) by opening each page directly; all match the
  evidence record's paraphrases and quotes.
- No chart or source-image asset was used. The evidence's three candidate
  source assets (the Bard GIF, the ESO image, the JWST image) were not
  essential to the argument once the exact quotes and a dated timeline carried
  the correction; adding image capture would have added risk without adding
  something the cited quotes don't already establish.
