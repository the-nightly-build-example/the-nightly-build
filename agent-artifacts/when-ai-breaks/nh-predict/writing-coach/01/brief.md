# writing-coach brief: when-ai-breaks/nh-predict (01)

Inputs:
  ../../commission.md — the incident (UnitedHealth / nH Predict), angle, boundaries, sources plan
  ../../editorial-direction.md — house + headline standard, press voice, lesson identity, series prompt
Output: ./voice-guide.md

Round focus: the genre is narrating an institutional-technology failure so the
mechanism is unmistakable — study exemplars who tell a systems-and-people
failure in order, keep allegation separate from fact, and make the causal step
land. Compile the "Recently used, do not reuse" list from the recent library's
deks and headings (`nb history --series when-ai-breaks`); the commission flags
the system-personification dek mold to use at most once.

Run-environment caveat: published library at `/home/user/library-checkout` for
`nb history`.
