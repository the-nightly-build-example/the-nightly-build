# Review brief 01 — editor — when-ai-breaks/gemini-image-generation

## Begin with these inputs
- this brief; `editorial-direction.md`; the exact writer brief `writer/01/brief.md`;
  `writing-coach/01/voice-guide.md`; `researcher/01/evidence.md`;
  `writer/01/draft-handoff.md` (original-work sentence at third read only);
  article `library/when-ai-breaks/gemini-image-generation.html`; `.nb-context/`.

Follow the **editor** skill (Skill: `editor`; fallback file). Three ordered reads;
surgical edits only.

## Watch especially for this piece
- **Skeptic:** this is a named-company, recent, contested event — check display text
  hardest. Verify every **date**, Prabhakar Raghavan's **exact title**, and the exact
  wording and attribution of the "completely unacceptable" quote against the record.
  Confirm the article marks clearly **Google's stated account vs. outside
  reconstruction** of the mechanism and does not overclaim internal details Google
  did not confirm. Confirm any impact/accusation has two independent confirmations,
  and that acknowledged outputs are distinguished from cherry-picked screenshots.
  Audit `data-nb-kind` (Google's posts and the verbatim-quoted memo are primary;
  outlet framing is secondary).
- **Cut:** the desk opens on a sharp factual jolt; there is no physical victim here,
  so cut any manufactured gravity. Cut culture-war vocabulary — the teaching is the
  engineering tradeoff. If a Background link to `../the-mechanics/instructions-are-data.html`
  was added, confirm that lesson is actually merged in library; if not, it must be
  removed. No colon-subtitle headline.
- **Reader:** does the reader leave understanding that one engineering choice
  (counter data bias with a hidden nudge) produced both the useful default and this
  failure (the promised contribution)? Compare to the original-work sentence.
- **Visual evidence:** if a source asset was used, inspect the image and rendered
  page — the crop must retain the evidence the argument spends; caption factual and
  cited; no external/hosted image. If it adds nothing, request its removal.

## Output
`editor/01/editorial-review.md` (Skeptic/Cut/Reader, edits, required work, decision).
Return `DONE editor <path>` or a REQUEST.
